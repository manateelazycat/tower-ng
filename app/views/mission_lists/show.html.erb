<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
  </head>
  <body class="fill-background-color" id="<%= controller.controller_name %>-<%= controller.action_name %>">
    <div class="menu-eventarea"
         data-controller="comment missiondistributor"
         data-action="click@window->comment#onClick click@window->missiondistributor#onClick">
    </div>
    
    <%= render "layouts/loading_dialog", message: "正在删除任务列表..." %>
    <%= render "layouts/mission_toolbar" %>
    <%= render 'layouts/header' %>

    <%= render "layouts/mission_distributor_menu" %>

    <%= link_to project_url(@project.hashid) do %>
      <div class="bread-crumbs-big">
        <%= @project.name %>
      </div>
    <% end %>

    <div class="right-float-menu-top"></div>

    <div data-controller="mission rightfloatmenu"
         data-rightfloatmenu-top-offset="80"
         class="board-box min-board-height right-float-menu-parent">
      <%= render "layouts/confirm_dialog", dialogid: "confirm-dialog", message: "确定要删除这个任务清单吗?", controller: "mission", action: "mission#deleteMissionList" %>

      <div class="right-float-menu-box">
        <div class="right-float-menu"
             data-target="rightfloatmenu.rightMenuArea"
             data-action="scroll@window->rightfloatmenu#onScroll">
          <ul class="right-float-menu-scrollarea">
            <li class="right-float-menu-item">
              <a href="#"
                 class="right-float-menu-normal-button"
                 data-action="mission#editMissionList">
                编辑
              </a>
            </li>
	    <% if team_administrator? %>
              <li class="right-float-menu-item">
		<a href="#"
                   class="right-float-menu-delete-button"
                   data-toggle="modal"
                   data-target="#confirm-dialog">
                  删除
		</a>
              </li>
	    <% end %>
          </ul>
        </div>
      </div>
      <%= render "mission_lists/create_mission_list", mission_list: @mission_list %>
    </div>

  </body>
</html>
