<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
  </head>
  <body class="content-page-body" id="<%= controller.controller_name %>-<%= controller.action_name %>">
    <%= render 'layouts/header' %>
    <div class="board-box">
      <div class="project-title"><%= Project.find_by_hashid(params[:id]).name %> </div>
      <div class="project-splitter-bold"></div>
      <div class="project-splitter-light"></div>

      <div class="flex-vcenter margins-vertical-normal">
	<button type="button" class="btn btn-default btn-sm">添加任务</button>
	<button type="button" class="btn btn-default btn-sm margins-horizontal-normal">添加任务清单</button>
      </div>
    </div>
  </body>
</html>
